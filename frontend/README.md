# Tic-Tac-Toe Frontend

This is a Vite + React + TypeScript starter for the Tic-Tac-Toe app.

Quick start

```bash
cd frontend
npm install
npm run dev
```

Make sure `VITE_BACKEND_URL` is set in `.env` or `.env.local` when running locally.
